{
  "name": "chat_app_notification",
  "version": "1.0.0",
  "description": "Notification service for project chat app",
  "main": "server.js",
  "scripts": {
    "build": "npx tsc",
    "start": "npx tsc && node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.5.3"
  }
}
